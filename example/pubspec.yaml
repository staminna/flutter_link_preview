name: flutter_link_preview_example
description: Demo for flutter_link_preview_update.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  flutter_link_preview_update:
    path: ../
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
